{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run 'main'",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build --bin main"
    }
    // Other configurations...
  ]
}